use nix

# Per-project Git identity + SSH-signed commits (helper defined in shells.nix).
# Uncomment and adjust; pass a third arg for a dedicated signing key.
# git_identity "Vandy Goel" "vandy.goel@work.com" "$HOME/.ssh/work_ed25519.pub"

export VIRTUAL_ENV=".venv/"
layout python3